MODEL:
  NAME: 'resnet50'
  DEVICE: 'cuda'
  CLASSES: 10
  PRETRAINED: True

DATA_DIR: 'dataset'

DATASET:
  IMAGESIZE: 224
  NAME: 'cifar10'

SOLVER:
  OPTIMIZER_NAME: 'SGD'
  MAX_EPOCHS: 200
  SCHEDULER_MVALUE: [60, 120, 160]
  LR: 0.1
  SCHEDULER: 'Cosine'
  
OUTPUT_DIR: 'output/'

